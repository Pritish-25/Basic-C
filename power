#include <stdio.h>
#include<math.h>
int main()
{
    int a,b;
    printf("enter the value of base no. ");
    scanf("%d", &a);
    printf("enter the power ");
    scanf("%d", &b);
    printf("the value is : %f", pow(a,b));
    return 0;
}

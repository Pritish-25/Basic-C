#include <stdio.h>

int main()
{
    char s[100];
    int count =0;
    scanf("%[^\n]s",s);
    for(int i=0;s[i];i++)
    {
        if(s[i] == 32)
        count ++;
    }
    printf("The total no. of words in given string are %d",count+1);
    

    return 0;
}

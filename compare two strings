#include <stdio.h>

int main()
{
    char a[100],b[100];
    scanf("%s",a);
    scanf("%s",b);
    int i;
    for(i=0;a[i];i++);
    int l1 = i;
    for(i=0;b[i];i++);
    int l2 = i;
    if(l1 == l2)
    printf("Equal strings");
    else
    printf("Unequal strings");

    return 0;
}
